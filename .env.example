# Convex deployment URL — populated automatically by `pnpm convex:dev` on first run.
VITE_CONVEX_URL=
# Optional for `pnpm seed:champions` in local dev (uses `convex login` token when unset).
# Set in CI/automation when running seed without a logged-in user token.
CONVEX_DEPLOY_KEY=
