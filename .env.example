# Convex deployment URL — populated automatically by `pnpm convex:dev` on first run.
VITE_CONVEX_URL=
# Optional: required only for `pnpm seed:champions`; never needed for normal dev or CI.
# CONVEX_DEPLOY_KEY=
