# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# AI assistants — local tooling and instructions, not project artifacts.
# Two exceptions are tracked:
#   - `.github/` (issue templates, workflows, copilot-instructions) so cloud tooling reads them
#   - `CLAUDE.md` as project-wide AI-agent documentation referenced by plan-issues
.claude/
.claude.json
.copilot/
AGENTS.md
.agents/
skills-lock.json
.cursor/
.cursorrules
.aider*
.continue/

# Environment / secrets
.env
.env.*
!.env.example

# Logs
*.log
logs/

# OS / temp
Thumbs.db
*.tmp

# Node / build output
node_modules/
dist/
dev-dist/
.vite/
*.tsbuildinfo

# TanStack Router auto-generated route tree
src/routeTree.gen.ts

# Convex auto-generated client (ignore everything inside, but whitelist the AI guidelines)
convex/_generated/*
# AI guidelines are mandatory reading for any Convex-touching code (see CLAUDE.md / AGENTS.md).
# Generated by `npx convex ai-files install`. Committed so Copilot and other agents read it from the repo.
!convex/_generated/ai/

# Test output
coverage/
.vitest-cache/
